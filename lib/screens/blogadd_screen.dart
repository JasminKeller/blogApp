import 'package:flutter/material.dart';

class BlogAddScreen extends StatelessWidget {
  const BlogAddScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Blog'),
      ),
      body: const Text('Miau'),
    );
  }
}
